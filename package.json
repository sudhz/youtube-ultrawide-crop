{
  "name": "youtube-ultrawide-crop",
  "version": "1.1.1",
  "description": "Enjoy YouTube without distracting black bars on your ultrawide monitor.",
  "type": "module",
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "homepage": "https://github.com/sudhz/youtube-ultrawide-crop#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sudhz/youtube-ultrawide-crop.git"
  },
  "bugs": {
    "url": "https://github.com/sudhz/youtube-ultrawide-crop/issues"
  },
  "keywords": [
    "chrome-extension",
    "youtube",
    "ultrawide",
    "zoom-to-fill",
    "manifest-v3"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/build.ts --watch",
    "typecheck": "tsc --noEmit",
    "test": "bun run build && bun test",
    "lint:firefox": "web-ext lint --source-dir dist-firefox",
    "validate": "bun run typecheck && bun run test && bun run lint:firefox",
    "pack:chrome": "web-ext build --source-dir dist-chrome --artifacts-dir artifacts --filename youtube-ultrawide-crop-v{version}-chrome.zip --overwrite-dest",
    "pack:firefox": "web-ext build --source-dir dist-firefox --artifacts-dir artifacts --filename youtube-ultrawide-crop-v{version}-firefox.zip --overwrite-dest",
    "pack:source": "bun run scripts/package-source.ts",
    "pack:all": "bun run pack:chrome && bun run pack:firefox && bun run pack:source",
    "release": "bun run validate && bun run pack:all"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/chrome": "^0.0.326",
    "@types/node": "^22.13.14",
    "esbuild": "^0.25.5",
    "fflate": "^0.8.3",
    "typescript": "^5.8.3",
    "web-ext": "10.3.0"
  }
}
